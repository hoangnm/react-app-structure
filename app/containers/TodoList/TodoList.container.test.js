import React from 'react';
import { shallow } from 'enzyme';
import { TodoListContainer } from './TodoList.container';

describe('TodoList Container', () => {
  // let _spies = {};
  // let _props, _wrapper;

  // beforeEach(() => {
  //   _props = {
  //     update: _spies.update = jest.fn(),
  //   };
  //   _wrapper = shallow(
  //     <TodoListContainer {..._props} />
  //   );
  // });

  test('Component exists', () => {
    expect(true).toBe(true);
  });
});
